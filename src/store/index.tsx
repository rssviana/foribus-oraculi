import store from './configure';
import reducers from './reducers';

import type { AppState, DomainState, DomainActionsTypes } from './types';

export type { reducers, AppState, DomainState, DomainActionsTypes };

export default store;
